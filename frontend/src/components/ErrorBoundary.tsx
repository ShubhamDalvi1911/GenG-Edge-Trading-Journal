import React from 'react';
export class ErrorBoundary extends React.Component<React.PropsWithChildren, {hasError:boolean}> { state={hasError:false}; static getDerivedStateFromError(){return {hasError:true};} render(){ if(this.state.hasError) return <div className="min-h-screen bg-[#05070d] p-6 text-slate-100"><div className="mx-auto mt-20 max-w-lg rounded-2xl border border-red-900/60 bg-slate-900 p-6"><div className="text-xs uppercase tracking-[0.24em] text-red-300">Unexpected application error</div><h1 className="mt-2 text-2xl font-semibold">GenG Edge needs a refresh</h1><p className="mt-2 text-sm leading-6 text-slate-400">The workspace hit an unexpected client-side error. Refreshing is safe and does not change your stored trades.</p><button className="mt-5 rounded-xl bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-500" onClick={()=>window.location.reload()}>Refresh application</button></div></div>; return this.props.children; } }
