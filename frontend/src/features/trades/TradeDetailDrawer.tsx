import { Pencil, Trash2, X } from 'lucide-react';
import type { Direction } from '../../types/workspace';
type Trade = { id:number; date:string; symbol:string; direction:Direction; entry:number; exit:number; size:number; strategy:string; session:string; pnl:number; rr:number; entryTime:string; exitTime:string; stopLoss?:number|null; takeProfit?:number|null };
const money=(v:number)=>`$${v.toFixed(2)}`;
export function TradeDetailDrawer({ trade, onClose, onEdit, onDelete }: { trade:Trade|null; onClose:()=>void; onEdit:(trade:Trade)=>void; onDelete:(trade:Trade)=>void }) { if(!trade)return null; return <div className="fixed inset-0 z-[60] flex justify-end" role="dialog" aria-modal="true" aria-label={`${trade.symbol} trade details`}><button type="button" aria-label="Close trade details" className="absolute inset-0 cursor-default bg-slate-950/70 backdrop-blur-sm" onClick={onClose}/><aside className="relative h-full w-full max-w-md overflow-y-auto border-l border-slate-800 bg-slate-900 p-5 shadow-2xl shadow-slate-950/50 sm:p-6"><div className="flex items-start justify-between gap-4"><div><div className="text-[10px] uppercase tracking-[0.24em] text-blue-400">Trade review</div><h2 className="mt-1 text-2xl font-semibold text-white">{trade.symbol} · {trade.direction}</h2><p className="mt-1 text-xs text-slate-500">{trade.date}</p></div><button type="button" onClick={onClose} className="rounded-lg border border-slate-700 p-2 text-slate-300 hover:border-slate-500" aria-label="Close"><X size={16}/></button></div><div className={`mt-6 text-3xl font-semibold ${trade.pnl >= 0 ? 'text-emerald-400' : 'text-red-400'}`}>{money(trade.pnl)}</div><div className="mt-6 grid grid-cols-2 gap-3">{[['Entry',trade.entry],['Exit',trade.exit],['Size',trade.size],['R:R',trade.rr],['Stop loss',trade.stopLoss??'—'],['Take profit',trade.takeProfit??'—']].map(([label,value])=><div key={String(label)} className="rounded-xl border border-slate-800 bg-slate-950/50 p-3"><div className="text-[11px] uppercase tracking-wider text-slate-500">{label}</div><div className="mt-1 font-medium text-slate-200">{typeof value==='number'?String(value):value}</div></div>)}</div><div className="mt-4 space-y-3">{[['Strategy',trade.strategy],['Session',trade.session],['Entry time',trade.entryTime],['Exit time',trade.exitTime]].map(([label,value])=><div key={String(label)} className="flex items-center justify-between gap-4 border-b border-slate-800/80 py-3 text-sm"><span className="text-slate-500">{label}</span><span className="text-right text-slate-200">{String(value)}</span></div>)}</div><div className="mt-6 flex gap-2"><button type="button" onClick={()=>{onEdit(trade);onClose();}} className="flex flex-1 items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-medium text-white hover:bg-blue-500"><Pencil size={15}/> Edit</button><button type="button" onClick={()=>{onDelete(trade);onClose();}} className="flex items-center justify-center gap-2 rounded-xl border border-red-800/70 px-4 py-2.5 text-sm font-medium text-red-300 hover:bg-red-950/30"><Trash2 size={15}/> Delete</button></div></aside></div>; }
