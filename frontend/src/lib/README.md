This folder is reserved for pure domain helpers and formatting utilities that should not depend on React rendering.

Keep financial calculations deterministic and unit-testable. UI components should consume these helpers rather than duplicating formulas.
